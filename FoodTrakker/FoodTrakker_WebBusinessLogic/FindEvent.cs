
using FoodTrakker_WebBusinessLogic.Model;
using FoodTrakker_WebBusinessLogic.Repository;

namespace FoodTrakker_WebBusinessLogic
{
    public class FindEvent
    {

        //public async Task<List<Event>> FindEventsForFoodTruck(int foodTruckID)
        //{
        //    var eventList = await new EventRepository().GetAsync();
        //    var eventsWithFoodTrucks = eventList.Where(e => e.FoodTrucksId != null).ToList();
        //    var events = eventsWithFoodTrucks.FindAll(e => e.FoodTrucksId.Any(i => i == foodTruckID));

        //    return events;
        //}
    }
}

